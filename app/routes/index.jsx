import { View, Text } from "react-native";

export default function RoutesScreen() {
    return (
        <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
            <Text>Routes will appear here</Text>
        </View>
    );
}
